/** @format */

import { useState } from "react";

const SearchBar = () => {
    return (
        <div className="w-[400px] h-[35px] mt-16 bg-slate-600 flex justify-center items-center rounded-full ">
            <div>Search Location</div>
        </div>
    );
};

export default SearchBar;
